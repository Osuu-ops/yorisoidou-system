name: build-artifacts
on:
  pull_request:
    branches: [ main ]
permissions:
  contents: read
jobs:
  build-artifacts:
    name: build-artifacts
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: build-artifacts (required check)
        run: |
          echo "build-artifacts required check: OK"
