name: MEP Writeback Bundle (Entry)

on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: "0 = auto latest merged PR"
        required: false
        default: "0"
      write_handoff:
        description: "Generate+Guard+Writeback HANDOFF_NEXT into Bundled"
        required: false
        default: "false"

jobs:
  call:
    uses: Osuu-ops/yorisoidou-system/.github/workflows/mep_writeback_bundle_dispatch.yml@main
    secrets: inherit
    with:
      pr_number: ${{ inputs.pr_number }}
      write_handoff: ${{ inputs.write_handoff }}

