name: MEP Auto PR + Gate (GitHub-only)

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "manual-only"