name: MEP LLM Dispatch Entry (ACTIVE)
on:
  workflow_dispatch:
    inputs:
      body:
        description: "payload body"
        required: true
        type: string
permissions:
  contents: read
concurrency:
  group: mep-llm-dispatch-entry-${{ github.event_name }}-${{ github.run_id }}
  cancel-in-progress: false
jobs:
  entry:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Make diff (smoke)
        run: |
set -euo pipefail
# =========================
# P3_SELF_HEAL (reason_code + retry + checks-wait)
# =========================
P3_MAX_RETRIES="${P3_MAX_RETRIES:-3}"
P3_BACKOFF_SEC="${P3_BACKOFF_SEC:-4}"
P3_CHECKS_TIMEOUT_SEC="${P3_CHECKS_TIMEOUT_SEC:-900}"
P3_CHECKS_POLL_SEC="${P3_CHECKS_POLL_SEC:-10}"
_reason() { echo "STOP_KIND=run: |
 REASON_CODE=${2} NEXT=${3}" | tee -a "$GITHUB_STEP_SUMMARY"; }
_sleep_backoff() { local n="run: |
"; sleep "$(( P3_BACKOFF_SEC * n ))"; }
_retry() {
  local what="run: |
"; shift
  local i=1
  while true; do
    echo "[P3] try=$i what=$what" | tee -a "$GITHUB_STEP_SUMMARY"
    if "$@"; then return 0; fi
    rc=$?
    if [ "$i" -ge "$P3_MAX_RETRIES" ]; then
      echo "[P3] FAIL what=$what rc=$rc" | tee -a "$GITHUB_STEP_SUMMARY"
      return $rc
    fi
    _sleep_backoff "$i"
    i=$((i+1))
  done
}
SSOT_PATH="docs/MEP/REQUIRED_CHECKS_SSOT.md"
_required_from_ssot() {
  [ -f "$SSOT_PATH" ] || return 1
  sed -n 's/^[[:space:]]*-[[:space:]]*"\(.*\)"[[:space:]]*$/\1/p' "$SSOT_PATH"
}
echo "[P3] P3_SELF_HEAL scaffold installed" | tee -a "$GITHUB_STEP_SUMMARY"
          set -euo pipefail
          mkdir -p mep
          echo "SMOKE ${{ github.run_id }} $(date -u +%Y-%m-%dT%H:%M:%SZ)" >> mep/_dispatch_entry_smoke.txt
      - name: Create PR (gh)
        env:
      P3_MAX_RETRIES: "3"
      P3_BACKOFF_SEC: "4"
      P3_CHECKS_TIMEOUT_SEC: "900"
      P3_CHECKS_POLL_SEC: "10"          GH_TOKEN: ${{ secrets.MEP_PR_TOKEN }}
        run: |
set -euo pipefail
# =========================
# P3_SELF_HEAL (reason_code + retry + checks-wait)
# =========================
P3_MAX_RETRIES="${P3_MAX_RETRIES:-3}"
P3_BACKOFF_SEC="${P3_BACKOFF_SEC:-4}"
P3_CHECKS_TIMEOUT_SEC="${P3_CHECKS_TIMEOUT_SEC:-900}"
P3_CHECKS_POLL_SEC="${P3_CHECKS_POLL_SEC:-10}"
_reason() { echo "STOP_KIND=run: |
 REASON_CODE=${2} NEXT=${3}" | tee -a "$GITHUB_STEP_SUMMARY"; }
_sleep_backoff() { local n="run: |
"; sleep "$(( P3_BACKOFF_SEC * n ))"; }
_retry() {
  local what="run: |
"; shift
  local i=1
  while true; do
    echo "[P3] try=$i what=$what" | tee -a "$GITHUB_STEP_SUMMARY"
    if "$@"; then return 0; fi
    rc=$?
    if [ "$i" -ge "$P3_MAX_RETRIES" ]; then
      echo "[P3] FAIL what=$what rc=$rc" | tee -a "$GITHUB_STEP_SUMMARY"
      return $rc
    fi
    _sleep_backoff "$i"
    i=$((i+1))
  done
}
SSOT_PATH="docs/MEP/REQUIRED_CHECKS_SSOT.md"
_required_from_ssot() {
  [ -f "$SSOT_PATH" ] || return 1
  sed -n 's/^[[:space:]]*-[[:space:]]*"\(.*\)"[[:space:]]*$/\1/p' "$SSOT_PATH"
}
echo "[P3] P3_SELF_HEAL scaffold installed" | tee -a "$GITHUB_STEP_SUMMARY"
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          BR="auto/mep_dispatch_entry_${{ github.run_id }}"
          git checkout -b "$BR"
          git add -A
          if git diff --cached --quiet; then
            echo "NO_DIFF: nothing to commit"
            exit 0
          fi
          git commit -m "mep: dispatch-entry smoke ${{ github.run_id }}"
          # force PAT auth for push (no prompt)
          git remote set-url origin "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git push -u origin "$BR"
          gh pr create --base main --head "$BR" \
            --title "MEP: apply (Dispatch Entry) run ${{ github.run_id }}" \
            --body "Applied by MEP LLM Dispatch Entry.`n- Event: ${{ github.event_name }}`n- Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"