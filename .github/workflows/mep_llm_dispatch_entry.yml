name: MEP LLM Dispatch Entry (OLD - SEALED)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  sealed_notice:
    runs-on: ubuntu-latest
    steps:
      - name: Old entry sealed
        run: |
          echo "STOP: This workflow is deprecated and sealed."
          echo "Use canonical v2 entry instead:"
          echo "  .github/workflows/mep_llm_dispatch_entry_v2.yml"
          echo "See docs:"
          echo "  docs/MEP/OLD_ENTRY_SEAL.md"
          exit 0