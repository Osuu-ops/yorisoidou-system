# Scope-IN (PR #1660)
## 目的
WORK_ID / ID_PROGRESS 系の診断フローで再発していた StrictMode クラッシュ（Count/if/Trim）を抑止し、監査と自動化の再現性を上げる。
## 変更点（最小）
- tools/*.ps1 の安全化：
  - '.Count' 参照を配列化＋Length系に寄せる（StrictMode耐性）
  - '-f (if(...))' の誤用を回避（式として評価される形に寄せる）
  - git出力の .Trim() 直撃を [string] キャストで安全化
## 影響範囲
- tools 配下のユーティリティのみ（仕様・Bundled内容を直接変更しない）
- 期待：ENTRY_ONCE で PARSE_ERROR/Count/if 起因の落ち方を再発させない
## 受入（DoD）
- PR checks: Scope Guard (PR) が PASS
- 	ools\mep_entry.ps1 -Once がクラッシュせず完走（重大エラーなし）
