# docs/MEP/SIGNED_BY_ALLOWLIST.yml
# 初期 Allowlist: 許可アカウント／bot を列挙します
# 変更ルール: Allowlist を変更する PR は Owner または Ops Lead の承認を最低 2 名必要とします（SPECS を参照）。

allowlist:
  - login: yorisoidoupdf
    roles: [ops-lead]
    note: "Primary ops lead account"
  - login: ci-bot
    roles: [ci-bot]
    note: "CI automation bot (allowed for FAST_LOOP if configured)"

teams:
  ops-leads-team: "org/ops-leads"
# SIGNED_BY_ALLOWLIST.yml
# List allowed "Signed-by" GitHub logins here.
# The verification step scans for lines containing: login: <github-login>
# - This file is intentionally simple: each entry is a YAML mapping with `login: "<name>"`.
# - I will inject the current authenticated user and include common bot accounts.
- login: "Osuu-ops"
- login: "dependabot[bot]"
- login: "github-actions[bot]"
