name: MEP Gate (PR)

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gate:
    name: MEP Gate (PR)
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "MEP Gate placeholder (registered)."
          echo "Next PR will restore full aggregation logic."
