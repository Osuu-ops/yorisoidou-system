name: MEP Loop Engine v2 (restore step1)
"on":
  push:
    branches: [ main ]
concurrency:
  group: mep-loop-engine-v2
  cancel-in-progress: false
permissions:
  contents: read
  actions: write
jobs:
  zone:
    runs-on: ubuntu-latest
    steps:
      - name: Smoke
        run: echo "ENGINE_V2_STEP1_OK"