name: MEP Issue Patch to PR (NO_LLM) [HELLO_MIN]
on:
  issue_comment:
    types: [created]
permissions:
  contents: read
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: hello
        run: echo "HELLO_MIN_OK"
