# TEST PR for metadata-format-check (Signed-by verification)

This is a synthetic test PR to validate the metadata-format-check workflow (Signed-by verification).
It intentionally includes a Signed-by line that matches the allowlist.

Plan: Validate metadata-format-check workflow (signed-by allowlist/membership/commit verification)
Apply: No production changes; add test file docs/MEP/TEST_PR.md
Recovery: CARD-FAST-01 (test)
Signed-by: yorisoidoupdf

Notes:
- This file and PR are test artifacts. Delete after verification.
